{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://waaseyaa.dev/schemas/codified_context_validation.json",
    "title": "CodifiedContextValidation",
    "description": "Schema for a codified context validation report",
    "type": "object",
    "required": ["session_id", "drift_score", "issues", "recommendation", "validated_at"],
    "properties": {
        "session_id": {
            "type": "string",
            "description": "Session this validation report belongs to"
        },
        "drift_score": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100,
            "description": "Computed drift score (0 = no drift, 100 = maximum drift)"
        },
        "issues": {
            "type": "array",
            "description": "List of detected drift issues",
            "items": {
                "type": "string"
            }
        },
        "recommendation": {
            "type": "string",
            "description": "Human-readable recommendation based on drift score"
        },
        "validated_at": {
            "type": "string",
            "format": "date-time",
            "description": "ISO 8601 timestamp when the validation was performed"
        }
    },
    "additionalProperties": false
}
